$(function(){
  var test_screen = $('#test_screen');
  test_screen.html("<%= escape_javascript(render 'new') %>").modal({
    autofocus: false,
    keyboardShortcuts: false,
    onHidden: function(){ $(this).empty(); }
  }).modal('show');

  testFormInit();
  $(window).off('resize', testFormInit).on('resize', testFormInit);

  answerSegInit();
  $(window).off('resize', answerSegInit).on('resize', answerSegInit);
});
